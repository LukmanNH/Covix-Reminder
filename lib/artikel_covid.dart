import 'package:flutter/material.dart';

class ArtikelCovid extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Row(
          children: [
            Column(
              children: [
                Text('ARTIKEL MENGENAI COVID-19'),
                Text('Content Covid 19')
              ],
            )
          ],
        )
      ],
    );
  }
}
